<resources>
    <!-- Основная тема приложения -->
    <style name="Theme.ProjectDreams" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/primary</item>
        <item name="colorOnPrimary">@color/on_primary</item>
        <item name="colorSecondary">@color/secondary</item>
        <!-- Другие цвета... -->
    </style>

    <!-- Тема для Splash Screen (обязательна для Android 12+) -->
    <style name="Theme.ProjectDreams.SplashScreen" parent="Theme.SplashScreen">
        <!-- Анимированный векторный значок -->
        <item name="windowSplashScreenAnimatedIcon">@drawable/logo_anim_vector</item>

        <!-- Цвет фона splash screen -->
        <item name="windowSplashScreenBackground">#00BCD4</item> <!-- Голубой фон -->

        <!-- Продолжительность анимации в миллисекундах -->
        <item name="windowSplashScreenAnimationDuration">1500</item>

        <!-- Тема, которая применится ПОСЛЕ splash screen -->
        <item name="postSplashScreenTheme">@style/Theme.ProjectDreams</item>
    </style>
</resources>